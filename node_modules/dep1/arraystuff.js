module.exports = {
    cycle: function (object) {
        return {
            take: function (number) {
                var array = [];
                for (var i = 0; i < number; i++) {
                    array.push(object);
                }
                return array;
            }
        }
    }
}